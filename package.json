{
  "name": "quarterly-ascension",
  "version": "0.1.0",
  "description": "10 AI middle-managers compete for VP using real Stellar payments via MPP",
  "type": "module",
  "workspaces": [
    ".",
    "cloud"
  ],
  "scripts": {
    "typecheck": "cd cloud && tsc --noEmit",
    "deploy:display": "[ \"$(git branch --show-current)\" = \"main\" ] || { echo 'Refusing: deploy:display must run on main (the Pages project is Direct Upload, so this script uploads whatever is on disk).'; exit 1; } && npx wrangler pages deploy cloud/display/public --project-name=quarterly-ascension --branch=main",
    "smoke:llm": "tsx scripts/smoke-test-llm.ts"
  },
  "dependencies": {
    "openai": "^6.34.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "dotenv": "^16.4.7",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "overrides": {
    "axios": "^1.15.3",
    "mppx": "^0.5.0",
    "@stellar/stellar-sdk": "^16.0.1",
    "ws": "^8.21.0"
  }
}
